import NavMenu from "@/app/features/ui/components/menu/NavBar";

const Header = () => {
    return (
        <header>
        <h1>PokeApp</h1>
        <NavMenu />
        </header>
    );
};

export default Header;
